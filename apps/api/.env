# ⚠️ 此文件已废弃，仅作为后备
# 
# 主要配置请使用项目根目录的 .env 文件
# 运行 start-dev.sh 会自动设置正确的数据库路径
#
# 此文件保留仅为兼容直接在 apps/api 目录运行 Prisma CLI 的情况

# 注意：这里使用相对路径，仅在从 apps/api 目录直接运行时生效
DATABASE_URL="file:./prisma/dev.db"

LLM_PROVIDER="qwen"
LLM_API_KEY="sk-xxx"
LLM_BASE_URL=""

OCR_ENGINE="paddleocr"

PORT=3000
NODE_ENV="development"
JWT_SECRET="dev-secret-change-me"

EXAM_EXPORT_ZIP_RETENTION_MINUTES=30
