# ExamForge 统一环境配置文件
# 复制此文件为 .env: cp .env.example .env
# 
# 注意：DATABASE_URL 会被 start-dev.sh 自动设置为正确的绝对路径
# 你不需要手动修改它

# ==================== 数据库配置 ====================
# 此路径会被 start-dev.sh 自动更新为绝对路径
# 固定位置: <项目根目录>/apps/api/prisma/dev.db
DATABASE_URL="file:./apps/api/prisma/dev.db"

# ==================== JWT 配置 ====================
# 请使用强密码，建议 32 位以上随机字符串
JWT_SECRET="your-super-secret-jwt-key-change-me"

# ==================== 服务端口 ====================
PORT=3000
NODE_ENV="development"

# ==================== AI/LLM 配置 ====================
# 支持的 Provider: qwen, openai, azure, local
LLM_PROVIDER="qwen"
LLM_API_KEY="your-llm-api-key-here"
LLM_BASE_URL=""
# LLM_MODEL="qwen-turbo"  # 可选，指定模型

# ==================== OCR 配置 ====================
# 支持的引擎: paddleocr, tesseract
OCR_ENGINE="paddleocr"

# ==================== 其他配置 ====================
# 导出 ZIP 文件保留时间（分钟）
EXAM_EXPORT_ZIP_RETENTION_MINUTES=30
